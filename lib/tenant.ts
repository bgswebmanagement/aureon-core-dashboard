// Demo mode has no real auth yet, so the tenant role is pinned to a single
// seeded property. Once Supabase Auth is wired in (see README.md), replace
// this with the signed-in user's linked property_id from their profile row.
export const DEMO_TENANT_PROPERTY_ID = "p1";
